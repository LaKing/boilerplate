// $app.uri() ,...
export default {
    uri: function(u) {
        if (!u) return "";
        return "https://" + ß.HOSTNAME + "/" + u;
    },
    url: "https://" + ß.HOSTNAME + "/",
    lang: "##&en en ##&hu hu ##",
};
